[project]
enable_telemetry = false
name = "PortfolioAI"

# Disable thread persistence — avoids the Chainlit 2.10.0 null metadata bug
# that causes "Cannot read properties of null (reading 'startsWith')"
[features]
prompt_playground = false
multi_modal = true
unsafe_allow_html = false

[UI]
name = "PortfolioAI"
show_readme_as_default = false
default_collapse_content = false
hide_cot = false
collapse_content = false

[meta]
generated_by = "1.3.0"
